async function loadStatus(){const box=document.getElementById('admin-status');if(!box)return;box.innerHTML='Loading marketing package status...';try{const res=await fetch('/api/admin-status');const data=await res.json();box.innerHTML=`<div class="status-list"><div><span class="status-pill">Role model: ${data.roles.join(' / ')}</span></div><div><strong>Agents:</strong> ${data.agents.join(', ')}</div><div><strong>Funnels:</strong> ${data.funnels.join(', ')}</div><div><strong>Channels:</strong> ${data.channels.join(', ')}</div><div><strong>Payments:</strong> ${data.payments.join(', ')}</div><div><strong>Mode:</strong> ${data.mode}</div></div>`;}catch(err){box.textContent='Status endpoint scaffold is present but not yet live-configured.';}}document.addEventListener('DOMContentLoaded',()=>{loadStatus();const btn=document.getElementById('refresh-admin');if(btn)btn.addEventListener('click',loadStatus);});