function json(res,status,payload){res.status(status).setHeader('Content-Type','application/json');res.end(JSON.stringify(payload));}function classifyLead(body){const text=`${body.intent||''} ${body.message||''} ${body.source||''}`.toLowerCase();if(/cater|hospitality|vendor|partner|wholesale/.test(text))return 'partner';if(/recipe|digital|cookbook/.test(text))return 'recipe-buyer';if(/truckee|local|bread|juice|preorder/.test(text))return 'local';return 'visitor';}export default async function handler(req,res){if(req.method!=='POST')return json(res,405,{message:'Method not allowed'});const body=req.body||{};const leadType=classifyLead(body);const recommendedFunnel={visitor:'visitor-to-order',local:'local-to-preorder','recipe-buyer':'recipe-buyer-to-bundle',partner:'partner-to-catering'}[leadType];return json(res,200,{message:'Lead intake scaffold processed',leadType,recommendedFunnel,nextStep:'Attach CRM, email provider, or automation runner to persist and route this lead.'});}