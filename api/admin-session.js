function json(res,status,payload){res.status(status).setHeader('Content-Type','application/json');res.end(JSON.stringify(payload));}export default async function handler(req,res){const supplied=(req.headers['x-admin-key']||req.query.key||'').toString();const superKey=(process.env.SUPERADMIN_KEY||'').trim();const adminKey=(process.env.ADMIN_KEY||'').trim();if(!supplied)return json(res,401,{authenticated:false,message:'Missing admin key'});if(superKey&&supplied===superKey)return json(res,200,{authenticated:true,role:'superadmin'});if(adminKey&&supplied===adminKey)return json(res,200,{authenticated:true,role:'admin'});return json(res,403,{authenticated:false,message:'Invalid admin key'});}