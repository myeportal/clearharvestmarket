import fs from 'fs';import path from 'path';function json(res,status,payload){res.status(status).setHeader('Content-Type','application/json');res.end(JSON.stringify(payload));}export default async function handler(req,res){try{const file=path.join(process.cwd(),'config','marketing-package.json');const raw=fs.readFileSync(file,'utf8');const cfg=JSON.parse(raw);return json(res,200,{roles:Object.keys(cfg.roles||{}),agents:cfg.agents||[],funnels:cfg.funnels||[],channels:cfg.channels||[],payments:cfg.payments||[],mode:cfg.automation?.defaultMode||'approval-first'});}catch(error){return json(res,500,{message:'Unable to load marketing package config',error:String(error)});}}